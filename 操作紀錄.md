# 操作紀錄（Operation Log）

> 記錄本專案所有資料庫操作與重要事件，避免遺漏。每次操作請在此追加。

---

## 2026-08-05 根因確認 + 修復「同 uid 連續下單覆寫」

### 根因（使用者重現確認）
同一瀏覽器（同一匿名 uid）**先登入後台**（admin.html `doLogin` 寫 `fgq_sessions/{uid}` 使其成為管理員），**再用同一 uid 下單**：
- 第二單對已存在的 order（doc id = uid）執行 `tx.set` = update → 原規則 `allow update: if isAdmin` **通過** → 覆寫第一單訂單、計數器再累加。
- 結果：訂單只剩第二筆（名稱3）、統計卻顯示 2人／5份、rate_limits/phones 僅 1 筆。

使用者重現步驟（機車活動 4444）：admin 登入 → 下單「名稱2、後三碼222、2台」（後台看得到）→ 同一視窗連續下「名稱3、後三碼333、3台」→ 統計 2人5份、後台只剩名稱3。

### 修復（雙保險）
1. **index.html** `_submit`：transaction 內 `tx.get(orderRef)`，訂單已存在即 `throw "你已經下過單了，每人限一單"`。
2. **firestore.rules** order update：`allow update: if isAdmin(eventId) && orderId != request.auth.uid`（管理員不可覆寫自己的訂單，仍可管理他人訂單）。

### 驗證（SDK 模擬 + REST 規則測試，全過）
- SDK 模擬 index.html transaction：第一單成功（counter 1/2），第二單被 `ORDER_EXISTS` 擋下，訂單保留 buyer=2、counter 不重複累加。
- REST：管理員 create 自己 order=200、update 自己 order=403、update 他人 order（後台收款）=200。
- 單元測試 16 通過。
- 測試活動 `testfix` 已清理（fgq_events 僅剩使用者重現用的 4444、fgq_admin/sessions/orders_phones 皆 0）。

---

## 2026-08-05 資料庫全面清除（第二次）

### 背景
使用者要求刪除所有測試資料（4444、repro4 等），重新測試「同瀏覽器連續下兩單」bug。

### 執行動作
用 firebase CLI 清空全部集合：
- `fgq_events`（4444 / repro4 活動及其 items / orders / counters / rate_limits）
- `fgq_admin`、`fgq_sessions`、`fgq_orders_phones`

### 驗證
四集合皆 0 docs。

### 重要發現（根因假設，強證據）
- 4444 同 uid 連續兩單之所以「成功」，是因為**同一個瀏覽器的匿名 uid 先登入過後台**（admin.html `doLogin` 寫 `fgq_sessions/{uid}` 使其成為管理員），再用同一 uid 下單 → 第二單 order `update` 走 `isAdmin` 規則通過 → 覆寫第一單訂單、計數器累加兩次。
- 這解釋了 4444 的 counter=2/5、order 僅 1 筆（buyer=3）、rate_limits/phones 各 1 筆、createTime 22s / updateTime 27s。

---

## 2026-08-05 資料庫全面清除

### 背景
使用者要求清空所有測試資料，重新測試。

### 執行動作
1. **REST 刪除 7777 活動**（eventId `6PALGvQ6lAJhSqQhlMwE`，密碼已知）
2. **firebase CLI 管理員權限清空全部集合**（不受 Security Rules 限制）：
   - `fgq_events`（含所有活動與子集合：items / orders / counters / rate_limits）
   - `fgq_admin`（管理密碼）
   - `fgq_sessions`（登入憑證）
   - `fgq_orders_phones`（手機後三碼）
3. **驗證**：四個集合皆為 0 docs

### 清除命令（未來可直接使用）
```powershell
firebase firestore:delete --project flash-group-buy-sk --recursive --force fgq_events
firebase firestore:delete --project flash-group-buy-sk --recursive --force fgq_admin
firebase firestore:delete --project flash-group-buy-sk --recursive --force fgq_sessions
firebase firestore:delete --project flash-group-buy-sk --recursive --force fgq_orders_phones
```

### 注意
- firebase CLI 需先登入（`firebase login`），且權限等同專案管理員
- `--recursive` 會連子集合一起刪，`--force` 跳過確認

---

## 2026-08-05 刪除誤建資料夾

刪除兩個因名稱打錯而誤建的資料夾（各含一份 probe.mjs 暫存腳本）：
- `快照糾團`（應為「快閃糾團」）
- `快閃糾派`（應為「快閃糾團」）

---

## 2026-08-04 rate_limits 管理員刪除修正

### 問題
`fgq_sessions` 規則禁止 update，管理員也無法刪除 `rate_limits`（rules 只允許本人寫入）。導致刪單/刪活動後 rate_limits 殘留成孤兒資料。

### 修正
- `firestore.rules`：`rate_limits` 增加 `allow delete: if isAdmin(eventId)`
- `admin.html` `deleteOrder`：刪單時一併刪除該 uid 的 `rate_limits`
- `admin.html` `deleteEvent`：刪活動時一併清除 `rate_limits` 子集合

---

## 2026-08-04 3333 活動調查結論

- 3333 顯示「2人5份」但訂單僅 1 筆（33/3份）＝ 舊版部署期間殘留資料
- 以真實瀏覽器（本地 + 線上 GitHub Pages）雙重驗證：現行版本同 uid 連下兩單 → 第 2 單被規則拒絕、計數器原子回滾，**不可能**產生錯置
- 已校正 3333 計數器為實際值（1人／3份），之後依使用者要求整個活動刪除

---

## 2026-08-04 移除「活動總量上限欄位」

- `admin.html`：刪除設定頁欄位、建立/儲存邏輯、品項上限聯動
- `index.html`：下單不再檢查活動總量（`limits.__total` 移除）
- 數量控制統一由品項管理的「數量上限」負責；統計區「份商品」仍顯示（用 `__total` counter）
- commit `cfe23da`
